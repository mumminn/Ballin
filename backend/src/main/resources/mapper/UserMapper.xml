<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.backend.domain.user.mapper.UserMapper">

    <resultMap id="UserResult" type="com.example.backend.domain.user.entity.UserEntity">
        <id     column="id"          property="id"          javaType="java.util.UUID" typeHandler="com.example.backend.global.mybatis.UUIDTypeHandler"/>
        <result column="email"       property="email"/>
        <result column="password"    property="password"/>
        <result column="name"        property="name"/>
        <result column="social_type" property="socialType"  javaType="com.example.backend.domain.user.entity.SocialType"/>
        <result column="created_dt"  property="createdDt"   javaType="java.time.Instant"/>
        <result column="updated_dt"  property="updatedDt"   javaType="java.time.Instant"/>
        <result column="created_id"  property="createdId"   javaType="java.util.UUID" typeHandler="com.example.backend.global.mybatis.UUIDTypeHandler"/>
        <result column="updated_id"  property="updatedId"   javaType="java.util.UUID" typeHandler="com.example.backend.global.mybatis.UUIDTypeHandler"/>
    </resultMap>

    <select id="findByEmail" parameterType="string" resultMap="UserResult">
        SELECT id, email, password, `name`, social_type,
        created_dt, updated_dt, created_id, updated_id
        FROM `user`
        WHERE email = #{email}
        LIMIT 1
    </select>

    <select id="findById" parameterType="string" resultMap="UserResult">
        SELECT id, email, password, `name`, social_type,
        created_dt, updated_dt, created_id, updated_id
        FROM `user`
        WHERE id = #{id,javaType=java.util.UUID,typeHandler=com.example.backend.global.mybatis.UUIDTypeHandler}
        LIMIT 1
    </select>

    <insert id="insertUser" parameterType="com.example.backend.domain.user.entity.UserEntity">
        INSERT INTO `user` (id, email, password, `name`, social_type, created_dt, updated_dt, created_id, updated_id)
        VALUES (
        #{id, jdbcType=BINARY, typeHandler=com.example.backend.global.mybatis.UUIDTypeHandler},
        #{email}, #{password}, #{name},
        #{socialType, typeHandler=org.apache.ibatis.type.EnumTypeHandler},
        #{createdDt, jdbcType=TIMESTAMP},
        #{updatedDt, jdbcType=TIMESTAMP},
        #{createdId, jdbcType=BINARY, typeHandler=com.example.backend.global.mybatis.UUIDTypeHandler},
        #{updatedId, jdbcType=BINARY, typeHandler=com.example.backend.global.mybatis.UUIDTypeHandler}
        )
    </insert>

    <update id="updateUserBasic" parameterType="com.example.backend.domain.user.entity.UserEntity">
        UPDATE `user`
        SET `name` = #{name},
        updated_id = #{updatedId, jdbcType=BINARY, typeHandler=com.example.backend.global.mybatis.UUIDTypeHandler},
        updated_dt = #{updatedDt, jdbcType=TIMESTAMP}
        WHERE id = #{id, jdbcType=BINARY, typeHandler=com.example.backend.global.mybatis.UUIDTypeHandler}
    </update>

</mapper>