<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.backend.domain.season.mapper.SeasonMapper">

    <select id="getSeason" resultType="com.example.backend.domain.season.dto.response.SeasonResponseDto">
        SELECT
            s.season_name AS SeasonName,
            c.category_name AS category,
            DATE_FORMAT(s.start_date, '%Y-%m-%d') AS startDate,
            DATE_FORMAT(s.end_date, '%Y-%m-%d') AS endDate
        FROM season s JOIN category c ON s.category_id = c.id;
    </select>
</mapper>